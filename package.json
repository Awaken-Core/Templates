{
  "name": "nox",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd client dev\"",
    "build": "bun run --cwd client build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}